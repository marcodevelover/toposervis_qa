#!/bin/bash

cd /opt/app

# run "npm i" only if folder node_module is empty
if [ ! -f "loaded.lock" ]; then

#rm -fr node_modules package-lock.json Gemfile.lock yarn.lock

#gem install bundler;

#bundle install;

#bundle exec rake db:migrate;

#npm i --quiet;

#yarn i --check-files

bundle exec rails server -p 3003

touch loaded.lock

fi;