<p id="notice"><%= notice %></p>

<h1>Document Types</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Deleted at</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @document_types.each do |document_type| %>
      <tr>
        <td><%= document_type.name %></td>
        <td><%= document_type.description %></td>
        <td><%= document_type.deleted_at %></td>
        <td><%= link_to 'Show', document_type %></td>
        <td><%= link_to 'Edit', edit_document_type_path(document_type) %></td>
        <td><%= link_to 'Destroy', document_type, method: :delete, data: { confirm: '¿Estas seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Document Type', new_document_type_path %>
