<%= form_with(model: admin_role, local: true) do |form| %>
  <% if admin_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_role.errors.count, "error") %> prohibited this admin_role from being saved:</h2>

      <ul>
        <% admin_role.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :deleted_at %>
    <%= form.datetime_select :deleted_at %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
