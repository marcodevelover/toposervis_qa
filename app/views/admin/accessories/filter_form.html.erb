  	 <%= search_form_for [:admin, @q], remote:true do |f| %> 
			<div class="modal-body">
				<div class="form-group">
					<div class="form-group"> 
						<label class="form-label"><%= t("accessories.name")%></label>
						<div class="form-group">
							<%= search_field_tag  "q[name_cont]",nil,class: "form-control", placeholder: t('accessories.name'), width: '100%'%>
						</div>
					</div>
					<div class="form-group"> 
						<label class="form-label"><%= t("accessories.description")%></label>
						<div class="form-group">
							<%= search_field_tag "q[description_cont]",nil,class: "form-control" , placeholder: t('accessories.description') %>
						</div>
					</div>				 
				</div>
			</div>
			<div class="modal-footer">
				<%= submit_tag t('search'),class: 'btn btn-primary waves-effect' %> 
				<button type="button" class="btn btn-danger waves-effect" data-dismiss="modal">Cancelar</button>
			</div>
		<% end %>
 