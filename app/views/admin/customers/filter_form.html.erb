  	 <%= search_form_for [:admin, @q], remote:true do |f| %> 
			<div class="modal-body">
				<div class="form-group">
					<div class="form-group"> 
						<label class="form-label"><%= t("customers.name")%></label>
						<div class="form-group">
							<%= search_field_tag  "q[name_cont]",nil,class: "form-control", placeholder: t('customers.name'), width: '100%'%>
						</div>
					</div>
					<div class="form-group"> 
						<label class="form-label"><%= t("customers.rfc")%></label>
						<div class="form-group">
							<%= search_field_tag "q[rfc_cont]",nil,class: "form-control" , placeholder: t('customers.rfc') %>
						</div>
					</div>
					<div class="form-group"> 
						<label class="form-label"><%= t("customers.business_name")%></label>
						<div class="form-group">
							<%= search_field_tag "q[business_name _cont]",nil,class: "form-control" , placeholder: t('customers.business_name') %>
						</div>  
					</div>
				</div>
			</div>
			<div class="modal-footer">
				<%= submit_tag t('search'),class: 'btn btn-primary waves-effect' %> 
				<button type="button" class="btn btn-danger waves-effect" data-dismiss="modal">Cancelar</button>
			</div>
		<% end %>
 